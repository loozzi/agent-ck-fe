import type { RouteBrowser } from '@/types/routes'

export const trainerRouterConfig: RouteBrowser[] = []

const TrainerNavigator = () => {
  return <div>TrainerNavigator</div>
}

export default TrainerNavigator
